{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build server",
            "command": "dotnet",
            "type": "process",
            "args": [
                "build",
                "${workspaceFolder}/src/Cynthia.Card.Server/Cynthia.Card.Server.csproj"
            ],
            "problemMatcher": "$msCompile"
        }
    ]
}